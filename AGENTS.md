# img-frame

## 项目简介
这是一个可以自动识别相片的参数如：拍摄相片的相机类型、焦距、快门速度、光圈值、iso，并为相片添加一个相框，相框显示以上参数

## 技术栈
- conda虚拟环境：img-frame
- Python 3.12
- PySide6：Windows 桌面界面
- Pillow：图片读取、合成、PNG 导出
- ExifRead：常规 EXIF 读取
- rawpy / LibRaw：RAW 图片解码
- ExifTool：完整读取和写入 EXIF、XMP、IPTC、厂商私有字段
- PyInstaller：生成 Windows 应用目录
- Inno Setup 6：制作安装程序

## 项目框架
- `img-test/`中的图片为测试图片

## 关键约束
- 回答和md文件使用中文，代码中使用英文
- 禁止更改`img-frame/`之外的文件
- git版本控制由我手动执行
- 所有程序只能使用`img-frame`虚拟环境
- 需要写入大段内容时，分段写入防止字数超出限制和超时
- 对项目进行改进时，如果有之前的代码，直接在现有的代码上修改；如果没有，则结合项目文件结构新建文件
- 添加了相框的相片保持画质不变
- 相框可以自定义，如：白色、黑色、磨砂感半透明等
- 字体统一为微软雅黑
- 每一个模块实现好后暂停后续模块的实现，由我进行审查